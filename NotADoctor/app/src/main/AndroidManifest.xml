<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rokly.notadoctor">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateEditActivity" />
        <activity android:name=".SymptomActivity" />
        <activity android:name=".MentionActivity"
            android:theme="@style/SlideTheme"/>
        <activity android:name=".DiagnoseActivity"
            android:theme="@style/SlideTheme"/>
        <activity android:name=".ConditionActivity"
            android:theme="@style/SlideTheme"/>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".FindADoctor"
            android:theme="@style/SlideTheme"></activity>


    </application>



    //TODO Navigation between the pages
    //TODO error handling for http requests in search mention, diagnose, get conditions detail, get places, get detail places.
    //TODO check for internetconection
    //TODO generell Material Desing text, aligment, appbar, Logo, elevation
    //TODO Widget
    //TODO add admob
    //TODO add Triage color
    //TODO Get rid of the yellow suggestions
    //TODO app is equeppied with a signing configuration
    //TODO Tablet layout
    //TODO Animations and style for buttons
    //TODO Animations for side change
    //TODO content description navigation using d-pad
    //TODO animate map marker if doctor is clicked
    //TODO decrease the min level.
    //TODO put header in the intercept



</manifest>